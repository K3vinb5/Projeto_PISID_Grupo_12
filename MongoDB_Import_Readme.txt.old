Hello!

Primeiramente inciem o mongodb, e criem uma base de dados com o nome que desejarem ${db_name}

Na vossa consola (não na consola do mongo), windows power shell, cmd ou o que for no vosso sistema operativo
Entrem na diretoria onde está a pasta com a nossa base de dados Mongodb, no caso "MongoDB"

mongorestore -d ${db_name} MongoDB/

